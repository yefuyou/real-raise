# Checkpoint：2026-07-30 文档治理基线

> 观察时间：2026-07-30 01:15（Asia/Shanghai）
> 生产 URL：`https://plain-wind-ae46.yefuyou2333.workers.dev/`
> 性质：事实快照，不代表所有状态都符合目标产品规格

## 1. Git 与 PR

| 项目 | 观察值 |
| --- | --- |
| `origin/main` | `6f66f25`，Merge PR #7 |
| 工作分支 | `hotfix/auth-and-data-chain` |
| 分支 HEAD | `234d2ce` |
| PR #7 | 已于 2026-07-30 01:01 合并 |
| 工作树 | 不干净；基线捕获时有 4 个未提交代码改动；本轮文档治理改动同样尚未提交 |

未提交且未纳入本 Checkpoint 事实源的文件：

- `src/api/analysisArtifacts.ts`
- `src/api/replayClient.ts`
- `src/components/InsightSection.tsx`
- `src/tests/runTests.ts`

这些改动试图改变 Replay 主正文的来源语义。它们不是 `234d2ce` 的内容，未经单独审阅不得作为主分支或生产事实。

本 Checkpoint 和同批文档在用户审阅前也不代表已进入主分支；正式提交时应补写文档 commit。

## 2. 生产运行时

2026-07-30 01:15 读取 `/health`：

```json
{
  "ok": true,
  "service": "real-raise-api",
  "liveEnabled": true,
  "judgeAccessConfigured": true,
  "partnerSsoConfigured": true
}
```

结论：

- 生产 Worker 在线。
- Partner SSO 被运行时声明为已配置。
- Judge 后端仍被运行时声明为已配置，与 ADR-0002 的目标状态不一致。
- 本次未能用 Wrangler 列出 deployment ID：本机默认 Node 为 18.18，当前 Wrangler 要求 Node 22+。
- “主分支已合并”不等于“生产已部署该 commit”；本 Checkpoint 没有给出这种结论。

## 3. 契约版本

| 契约 | 当前仓库值 |
| --- | --- |
| 确定性计算 | `living-cost.v2` |
| Prompt | `diagnosis.v2.1-agent-act` |
| 上下文 | `real-raise.context.v2` |
| Agent 模式 | `act` |
| 诊断包 | `real-raise.diagnostic-packet.v1` |
| 情景矩阵 | `real-raise.scenario-matrix.v1` |
| 分析清单 | `real-raise.analysis.v2` |
| Replay manifest | `replay-manifest.v2` |

## 4. 当前产品路径

### 仓库 UI

- 本地确定性算表。
- 未登录与已登录都可见的显式 Replay 入口。
- Partner SSO 登录后可生成用户实时任务。
- Partner 登录用户可选择平台默认、DeepSeek V4 Flash 或 DeepSeek V4 Pro。
- Judge 组件文件仍存在，但 `InsightSection` 不挂载它。
- BYOK 产品入口已删除。

### Worker

- Partner SSO 路径存在。
- Judge session、token、项目 Key 路径仍存在。
- 未登录请求仍可能进入 Judge 模式判断后被认证拒绝。

## 5. Replay 清单

四个包均录制于 2026-07-26，供应商 Task 未重新录制：

| 场景 | Task ID | 录制口径 | Prompt 版本 |
| --- | --- | --- | --- |
| `take-home-raise-shrinks` | `6147a8d1-4bb9-416c-a664-ae54092fcc26` | 旧收入/支出增速比 | 未记录 |
| `raise-and-fixed-costs` | `a2083df0-0792-4b1e-8d04-03d8cc0d5028` | 旧收入/支出增速比 | 未记录 |
| `comfortable-raise` | `90c66681-eba2-4feb-816d-90ec416a2122` | 旧收入/支出增速比 | 未记录 |
| `payslip-raise-and-fixed-costs` | `2a735498-f2ec-448f-b839-639ddce26c4f` | 旧收入/支出增速比 | 未记录 |

`replay.v2` 表示双请求和原件审计包装，不表示这些任务使用当前 Prompt。

## 6. 预设中间值

### 到手模式

| 预设 | 收入变化 | 房租变化 | 月结余变化 | 观察 |
| --- | ---: | ---: | ---: | --- |
| 到手涨薪缩水 | 8,000 → 8,400 | 1,800 → 2,200 | 约 -49 | 涨薪正好被房租吃完，再叠加日常 CPI |
| 涨薪被固定支出分走 | 9,500 → 12,500 | 2,600 → 4,200 | 约 +1,345 | 仍明显改善；“固定支出”实际只有房租 |
| 涨薪真的变轻松 | 11,000 → 13,000 | 2,800 → 2,900 | 约 +1,841 | 房租稳定，结果明显改善 |

### 工资条示例

默认通用估算：

- 税前 `10,000 → 11,000`
- 到手 `7,667 → 8,382`
- 税前增加 `1,000`
- 到手增加 `715`
- 涨薪保留率 `71.5%`
- 默认公积金比例 `12%`

## 7. 数据覆盖

- 全国：2021–2025 历史收入、支出和 CPI。
- 全国：2026H1 八类 CPI。
- 合肥：2024 历史综合与八类；2026H1 分类回退全国。
- 北京、深圳：2026H1 综合与八类。
- 上海：2026H1 综合；202606 八类月度样本。
- 城市目录：30 城目录不等于 30 城完整数据。

详见 [数据资产目录](../data/README.md)。

## 8. 测试证据

- PR #7 最新 GitHub Actions `verify`：通过。
- 此前对 `234d2ce` 的本地应用测试：45/45。
- Worker 测试：此前通过。
- 生产变量构建：此前通过。
- 当前 4 个未提交代码改动没有作为本 Checkpoint 的通过证据。

## 9. 已知漂移

1. Judge 后端与目标产品路径冲突。
2. 输入变化后的 `completed-stale` 操作入口不够可见。
3. 重新分析会先清空旧报告，缺少失败回退。
4. 预设案例缺少设计依据，工资条预设会发生模式语义错位。
5. Replay 不是当前 Prompt 录制。
6. 旧技术文档仍包含 Judge、BYOK 或“尚未部署”等过时陈述。

## 10. 下一步边界

当前决定先冻结功能扩展，完成文档治理和状态机评审。未确认前不继续：

- 删除 Judge 后端；
- 修改预设；
- 重新录制 Replay；
- 改重新分析状态；
- 部署生产。
