# ADR-0005：模型选择只属于 Partner 实时任务

> 状态：已接受
> 决策日期：2026-07-30

## 决定

- 已登录用户可选平台默认、DeepSeek V4 Flash、DeepSeek V4 Pro。
- 回放忽略当前模型选择，严格使用录制任务。
- 未登录状态不显示可误解为 BYOK 的模型调用入口。
- 移除 BYOK 或 Judge 时不得顺带删除 Partner 模型选择。

## 原因

模型是实时任务的质量与额度选择，不是认证模式。此前把模型控件和旧 BYOK/Judge UI 放在同一区域，导致删除旧模式时误删模型选择。
